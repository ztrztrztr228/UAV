# 基于深度强化学习的三维无人机小区轨迹规划项目提纲

## 1. 项目背景

本项目面向小区场景下的无人机自主航线规划问题。目标是让无人机在给定三维起点和三维目标点后，能够在避开建筑物、边界和禁飞区的前提下，自动规划一条可行飞行轨迹，并尽可能缩短飞行距离和飞行时间。

相比二维路径规划，三维路径规划增加了高度维度。无人机不仅可以在水平面内绕开建筑物，也可以通过爬升、下降等动作从建筑物上方或侧方通过，因此状态空间、动作空间、碰撞检测和可视化都需要扩展到三维。

## 2. 代码结构

```text
UAV/
├── uav_drl_path_planning.py       # 主函数和命令行入口
├── uav_drl/
│   ├── __init__.py                # 包导出
│   ├── actions.py                 # 三维动作空间定义，26 个方向 + hover
│   ├── config.py                  # 三维地图、长方体障碍物和环境参数
│   ├── environment.py             # 三维环境、状态、奖励、碰撞检测
│   ├── model.py                   # DQN 神经网络
│   ├── agent.py                   # DQN 智能体和经验回放池
│   ├── training.py                # 训练和评估函数
│   ├── visualization.py           # 训练曲线、三维轨迹图、轨迹 CSV 保存
│   └── utils.py                   # 随机种子、三维坐标解析等工具函数
└── UAV_DRL_项目提纲.md             # 项目说明和报告提纲
```

想改地图看 `config.py`，想改奖励函数看 `environment.py`，想改网络结构看 `model.py`，想改训练流程看 `training.py`。

## 3. 总体流程

```text
构建三维小区地图
    ↓
定义无人机三维状态空间和动作空间
    ↓
设计三维奖励函数和碰撞检测
    ↓
使用 DQN 神经网络估计各动作价值
    ↓
智能体与三维环境交互，收集经验
    ↓
经验回放 + Double DQN 更新网络
    ↓
评估训练后的策略并保存三维轨迹
```

## 4. 三维地图与环境建模

地图使用三维坐标系表示，默认范围为：

```text
x: 0 ~ 100
y: 0 ~ 100
z: 0 ~ 30
```

其中 `x/y` 表示水平位置，`z` 表示高度。

地图尺寸在 `UAVEnvConfig` 中定义：

```python
map_width = 100.0
map_height = 100.0
map_altitude = 30.0
```

建筑物和禁飞区使用长方体表示：

```python
BoxObstacle(xmin, ymin, zmin, xmax, ymax, zmax, name)
```

例如：

```python
BoxObstacle(18, 12, 0, 34, 43, 18, "building_a")
```

表示一栋从地面 `z=0` 延伸到 `z=18` 的建筑物。

## 5. 三维状态空间

状态空间共 40 维，作为神经网络输入。

| 状态内容 | 维度 | 作用 |
| --- | ---: | --- |
| 当前三维坐标 x/y/z | 3 | 表示无人机当前位置 |
| 目标相对向量 dx/dy/dz | 3 | 表示目标点相对于当前位置的方向和距离 |
| 到目标点三维距离 | 1 | 提供剩余任务距离 |
| 朝向目标点单位向量 | 3 | 提供目标方向信息 |
| 上一步三维移动方向 | 3 | 用于学习平滑轨迹 |
| 26 方向三维雷达距离 | 26 | 感知周围建筑物、边界和高度限制 |
| 当前 episode 进度 | 1 | 表示剩余时间压力 |

所有坐标和距离都会归一化，使神经网络训练更稳定。

## 6. 三维动作空间

动作空间为 27 个离散动作：

```text
26 个三维邻接方向 + 1 个 hover
```

三维方向来自：

```text
dx, dy, dz ∈ {-1, 0, 1}
```

去掉 `(0, 0, 0)` 后得到 26 个方向，包括：

- 水平移动：east、north、south_west 等；
- 垂直移动：up、down；
- 三维斜向移动：north_east_up、south_west_down 等；
- 悬停：hover。

所有方向都会归一化，因此斜向飞行和直线飞行的实际步长一致。

## 7. 三维碰撞检测

碰撞检测包括：

1. 是否飞出水平边界；
2. 是否飞出高度范围；
3. 是否进入任意长方体建筑物；
4. 飞行线段中途是否穿过建筑物。

代码不是只检测动作终点，而是把当前点到下一点之间的三维线段离散采样，只要任意采样点碰撞，就认为该动作不安全。

## 8. 奖励函数设计

奖励函数仍采用密集奖励：

```text
总奖励 =
    接近目标奖励
  - 剩余距离惩罚
  - 每步时间惩罚
  - 悬停惩罚
  - 急转弯惩罚
  - 靠近障碍物惩罚
  - 轻微高度惩罚
  + 到达目标奖励
  - 碰撞惩罚
  - 超时惩罚
```

其中：

- 接近目标奖励鼓励无人机每一步缩短三维距离；
- 近障碍物惩罚鼓励保持安全净空距离；
- 高度惩罚避免无人机无意义地飞得过高；
- 到达目标奖励给较大的正反馈；
- 碰撞和超时给较大的负反馈。

## 9. DQN 算法

本项目使用 DQN，Deep Q-Network，学习动作价值函数：

```text
Q(s, a)
```

其中：

- `s` 是 40 维三维环境状态；
- `a` 是 27 个离散动作之一；
- `Q(s, a)` 表示在状态 `s` 下选择动作 `a` 后的未来累计奖励估计。

网络输入为 40 维状态，输出为 27 个动作对应的 Q 值。

## 10. 训练机制

### 10.1 epsilon-greedy 探索

训练初期多探索，后期逐渐更多使用当前最优策略：

```text
epsilon 从 1.0 衰减到 0.05
```

### 10.2 经验回放

智能体保存每一步经验：

```text
(state, action, reward, next_state, done)
```

训练时随机采样 batch，打破连续轨迹相关性。

### 10.3 Double DQN

代码使用两套网络：

- `policy_net`：选择下一状态动作；
- `target_net`：估计该动作价值。

这样可以降低普通 DQN 对 Q 值的过高估计。

## 11. 运行命令

### 11.1 快速测试

```powershell
python .\uav_drl_path_planning.py --episodes 3 --eval-episodes 1 --no-plots
```

### 11.2 正式训练三维模型

```powershell
python .\uav_drl_path_planning.py --episodes 1500 --eval-episodes 20 --visualize
```

### 11.3 指定三维起点和目标点

```powershell
python .\uav_drl_path_planning.py --episodes 1500 --start-x 5 --start-y 5 --start-z 8 --target-x 92 --target-y 88 --target-z 12 --visualize
```

### 11.4 加载已有模型进行三维规划

```powershell
python .\uav_drl_path_planning.py --skip-train --load-model .\outputs\uav_dqn.pt --start-x 5 --start-y 5 --start-z 8 --target-x 92 --target-y 88 --target-z 12 --visualize
```

如果只输入 `x/y` 而不输入 `z`，代码会使用 `--default-altitude` 的高度作为默认 z 值。

## 12. 输出结果

默认输出目录为：

```text
outputs
```

主要输出文件包括：

| 文件 | 含义 |
| --- | --- |
| `uav_dqn.pt` | 训练后的三维 DQN 模型参数 |
| `training_curve.png` | 训练奖励、成功率、损失曲线 |
| `best_trajectory.csv` | 最佳三维飞行轨迹坐标 x/y/z |
| `best_trajectory.png` | 最佳三维飞行轨迹图 |

## 13. 实验指标

可以从以下指标评价效果：

- 成功率；
- 碰撞率；
- 平均奖励；
- 最终三维距离；
- 三维路径长度；
- 飞行步数；
- 轨迹平滑程度；
- 平均飞行高度。

## 14. 后续改进方向

1. 增加连续动作空间算法，如 DDPG、TD3、SAC；
2. 加入动态障碍物，如行人、车辆、其他无人机；
3. 使用真实三维建筑地图，而不是手工长方体障碍物；
4. 加入电量消耗、最大爬升率、最大速度等真实飞行约束；
5. 使用课程学习，从简单低楼层地图逐渐训练到复杂高楼地图；
6. 加入多航点任务，实现三维巡航路径规划。

## 15. 小结

当前代码已经从二维轨迹规划扩展为三维轨迹规划。地图由二维矩形障碍物升级为三维长方体建筑，状态空间加入高度和三维雷达，动作空间扩展为 26 个三维方向加悬停，碰撞检测也升级为三维线段检测。智能体仍使用 DQN 和经验回放进行训练，最终可以根据三维目标点输出飞行动作序列，并生成三维轨迹图。

## 16. 第二阶段：动力学强化学习

第二阶段不再把动作解释为固定长度的位置跳变，而是解释为 26 个方向的最大加速度指令和一个零加速度指令。环境以固定时间步执行速度和位置积分，并对速度进行硬限幅。

状态空间扩展为 46 维，新增当前速度、实际加速度、速度占比、加速度占比和制动裕度。奖励函数新增加速度、急动度、速度占比、速度限幅、速度朝向和制动距离风险项；到达目标不仅要求位置进入目标半径，还要求末速度低于阈值。

评估阶段直接输出 RL 积分产生的等时间轨迹，不再使用事后平滑来构造动力学量。验证模块独立复算以下条件：

- 全轨迹点和相邻线段无碰撞且没有越界；
- 最大速度、最大加速度和最大急动度满足配置；
- 位置、速度与加速度满足离散时间积分关系；
- 末端位置和末端速度同时满足到达条件。

验证结果保存到 `trajectory_validation.json`，完整轨迹点保存到 `best_timed_trajectory.csv`。
